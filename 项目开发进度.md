# MindChat Knowledge Studio 项目开发进度

> 工作空间：`D:\ceshi\huabu\mindchat-knowledge-studio`
> 参考项目：qibu、knowledge-canvas
> 原项目策略：只读参考，不修改原项目代码

## 零、环境说明

| 项 | 内容 |
|---|---|
| 会话类型 | 本地 Windows |
| 实际落盘路径 | `D:\ceshi\huabu\mindchat-knowledge-studio` |
| 原项目影响 | 尚未修改；新项目独立目录 |
| 进度真源 | 本文件 |

## 一、当前执行位置

| 项 | 内容 |
|---|---|
| 当前项目 | MindChat Knowledge Studio |
| 当前里程碑 | M1 qibu 基座迁移 |
| 下一步 | 在 `client/` 基座上逐步接入 M2 四格式统一导入，不再扩展旧 `web/` 原型 |
| 计划状态 | 进行中 |
| 完成度 | 1 / 8 步（基座迁移完成，回归待继续） |
| 最近更新 | 2026-09-09 |

## 二、里程碑进度总表

| 里程碑 | 目标 | 状态 | 验收结论 |
|---|---|---|---|
| M0 | 文档与骨架 | ✅ 完成 | 3 份文档已落盘；原 qibu 跟踪代码无 diff |
| M1 | qibu 基座迁移 | 进行中 | qibu 完整前端已复制到 `client/`，静态资源与语法通过；浏览器功能回归待补 |
| M2 | 四格式导入 | 待开始 | — |
| M3 | 双层画布模型 | 待开始 | — |
| M4 | 本地 Bridge | 待开始 | — |
| M5 | 云端模型通道 | 待开始 | — |
| M6 | 工作区能力 | 待开始 | — |
| M7 | 收尾与独立交付 | 待开始 | — |

## 三、M0 文档决策

1. 对话顺序边和知识语义边使用两个独立字段，禁止互相覆盖。
2. 四种文件统一进入同一个导入适配层。
3. 浏览器只调用本地 bridge；bridge 再路由本地 Ollama 或云端 OpenAI 兼容接口。
4. 原 qibu 与 knowledge-canvas 保持只读，新的项目单独开发、单独测试、单独提交。

## 四、M0 验收记录

### 已完成

- 已读取 qibu 的 README、架构、导入器、存储和 API 逻辑。
- 已读取 knowledge-canvas 的 README、白板、拆分器、提取器、关联器和模型客户端。
- 已确认四种输入和双模型通道的产品边界。
- 已创建本开发文档和本进度文档。

### 待验证

- 新项目代码骨架将在 M1 开始时创建。
- 父目录 Git 会显示新项目目录为未跟踪，这是新项目文件出现的正常结果；原 qibu 的既有跟踪代码无 diff。

## 五、遗留问题

1. 云端模型的具体 endpoint、model 和临时 API Key 尚未指定，先实现可配置接口和 dry-run。
2. 新项目目录是否需要单独推送到哪个远程仓库，待代码骨架完成后再确定。

## 六、M1 四格式导入详情

### 改动文件

- `D:\ceshi\huabu\mindchat-knowledge-studio\bridge\importers.py`
  - 新增统一 `Message`、`ImportResult` 数据模型。
  - 支持 `.txt`、`.md`、`.json`、`.docx`。
  - 兼容 `User/Assistant`、`you asked/gemini response`、中文标签、Gemini `contents[].type=text`、顶层 JSON 数组和 knowledge-canvas `cards/graph`。
  - DOCX 只读取 `word/document.xml` 正文，不执行宏和外部资源。
- `D:\ceshi\huabu\mindchat-knowledge-studio\bridge\__init__.py`
- `D:\ceshi\huabu\mindchat-knowledge-studio\tests\test_importers.py`
  - 新增 5 个回归用例。

### 验证输出

```text
Ran 5 tests in 0.029s
OK

.txt 8 False
.md 8 False
.json 8 False
.docx 2 False
```

含义：附件中的 txt/md/json 均解析为 8 条消息（4 轮问答）；docx 样例解析为 2 条消息；知识图谱 JSON 返回结构化 graph 而不是消息数组。

### 自检 8 项

1. ✅ 字段与《开发文档.md》第 5、6 节一致。
2. ✅ 仅使用 Python 标准库，无新增第三方依赖。
3. ✅ 文件位于新项目独立目录。
4. ✅ JSON 消息、Gemini contents、cards/graph 分支均有明确归一化路径。
5. ✅ 本阶段只做导入适配，没有提前实现画布或模型调用。
6. ✅ 单元测试和真实四附件回归均实跑通过。
7. ✅ DOCX 安全边界（只读正文、不执行宏）已明确实现。
8. ✅ 关键类、函数和安全决策均有中文 docstring/注释。

### 遗留问题

无。M2 可以开始。

## 八、M3 本地 Bridge 详情

### 改动文件

- `D:\ceshi\huabu\mindchat-knowledge-studio\bridge\server.py`
  - 新增独立 HTTP 服务：`/health`、`/api/import/normalize`、`/api/knowledge/extract`、`/api/knowledge/link`、`/api/knowledge/analyze`。
  - 浏览器只需调用本地 Bridge，不直接处理 DOCX 或模型跨域。
- `D:\ceshi\huabu\mindchat-knowledge-studio\bridge\pipeline.py`
  - dry-run 知识卡片提取和关键词语义关联。
  - 本地 Ollama JSON 调用通道。
- `D:\ceshi\huabu\mindchat-knowledge-studio\bridge\importers.py`
  - 新增 `parse_bytes`，供 HTTP 上传调用。
- `D:\ceshi\huabu\mindchat-knowledge-studio\bridge\README.md`
- `D:\ceshi\huabu\mindchat-knowledge-studio\tests\test_pipeline.py`

### 验证输出

```text
Ran 7 tests in 0.033s
OK
health: ok / mindchat-knowledge-studio-bridge
normalize messages: 4
analyze cards: 2, semanticEdges: 0
```

说明：2 张样例问答正确提取为 2 张知识卡片；样例关键词没有达到两个重合词，因此没有虚构语义边。

### 自检 8 项

1. ✅ API 与《开发文档.md》第 4、7 节一致。
2. ✅ 使用 Python 标准库 HTTP 服务和 urllib，无第三方依赖。
3. ✅ Bridge 位于新项目独立目录。
4. ✅ 输入、卡片、语义边字段与统一模型一致。
5. ✅ 本阶段只实现本地/dry-run，云端留给 M4。
6. ✅ 单元测试、Python 编译、HTTP health/normalize/analyze 均实跑通过。
7. ✅ 不支持的 provider 会明确报错，不静默回退。
8. ✅ Bridge、管线和安全边界均有中文注释/docstring。

### 遗留问题

1. 当前机器未用真实 Ollama 模型做联网回归；已完成本地通道代码和 dry-run 验证。

## 九、M4 云端模型通道详情

### 改动文件

- `D:\ceshi\huabu\mindchat-knowledge-studio\bridge\pipeline.py`
  - 新增 OpenAI 兼容 JSON 请求。
  - 支持 endpoint 根地址、`/v1` 和完整 `/chat/completions`。
  - `extract_cards`、`link_cards` 支持 `provider=cloud`。
- `D:\ceshi\huabu\mindchat-knowledge-studio\bridge\server.py`
  - 将 `api_key` 透传到当前请求，不写入文件或日志。
- `D:\ceshi\huabu\mindchat-knowledge-studio\bridge\README.md`
- `D:\ceshi\huabu\mindchat-knowledge-studio\tests\test_cloud.py`

### 验证输出

```text
Ran 9 tests in 0.044s
OK
health: ok / mindchat-knowledge-studio-bridge
link dry-run: 1 edge
cloud mock: URL 与 Authorization 头验证通过
```

### 自检 8 项

1. ✅ 通道与《开发文档.md》第 7 节一致。
2. ✅ 使用标准库 urllib，无新增 SDK 依赖。
3. ✅ 云端代码只在新项目目录。
4. ✅ endpoint、model、api_key 字段路径明确。
5. ✅ 本阶段只增加云端通道，不扩展工作区功能。
6. ✅ 9 个单测、health 和 link 接口实跑通过。
7. ✅ API Key 仅验证请求头，不落盘。
8. ✅ 关键请求和安全边界均有中文说明。

### 遗留问题

1. 尚未使用用户真实云端 Key 做联网回归；需要用户提供临时 endpoint/model/key 后再测。

## 十、M5 工作区能力详情

### 改动文件

- `D:\ceshi\huabu\mindchat-knowledge-studio\web\workspace.js`
  - 新增独立 WorkspaceStore：多画布、切换、重命名、删除、搜索、localStorage 和备份恢复。
- `D:\ceshi\huabu\mindchat-knowledge-studio\web\index.html`
  - 增加画布选择、新建和搜索控件。
- `D:\ceshi\huabu\mindchat-knowledge-studio\web\app.js`
  - 接入工作区状态，拖动后自动保存，搜索结果过滤。
- `D:\ceshi\huabu\mindchat-knowledge-studio\web\style.css`
- `D:\ceshi\huabu\mindchat-knowledge-studio\tests\test_workspace.js`

### 验证输出

```text
workspace smoke: create/search/switch/delete/backup restore passed
board model smoke: 2 conversation edges + 2 semantic edges -> 3 visible edges
web index HTTP: 200
```

### 自检 8 项

1. ✅ 工作区能力对应《开发文档.md》第 8 节 M5。
2. ✅ 未引入第三方框架或数据库。
3. ✅ 工作区存储使用新项目独立键。
4. ✅ 画布字段保留两类边和分组/批注扩展位。
5. ✅ 未修改原项目工作区或 localStorage 键。
6. ✅ Node smoke 和本地 HTTP 页面验证通过。
7. ✅ 单画布不能删除的边界有明确错误提示。
8. ✅ 关键工作区操作含中文注释。

### 遗留问题

无。M6 可以开始。

## 十一、M6 收尾详情

### 改动文件

- `D:\ceshi\huabu\mindchat-knowledge-studio\.gitignore`
- `D:\ceshi\huabu\mindchat-knowledge-studio\start-bridge.bat`
- `D:\ceshi\huabu\mindchat-knowledge-studio\README.md`
- 独立仓库首个提交：`4530e50 feat: scaffold standalone mindchat knowledge studio`

### 全量验证输出

```text
Ran 9 tests in 0.039s
OK
board model smoke: 2 conversation edges + 2 semantic edges -> 3 visible edges
workspace smoke: create/search/switch/delete/backup restore passed
bridge health: ok
frontend HTTP: 200
original qibu tracked code: clean
```

### 隔离结论

- 原 qibu 的跟踪代码无 diff。
- 原 knowledge-canvas 没有被新项目运行时依赖修改。
- 新项目有自己的 `.git`，当前分支为 `master`，尚未绑定远程仓库。

### 自检 8 项

1. ✅ 所有实现均可追溯到《开发文档.md》里程碑。
2. ✅ 未引入文档外的第三方技术栈。
3. ✅ 新项目目录结构与文档一致。
4. ✅ API、数据模型、启动方式已写入 README。
5. ✅ 未修改原 qibu/knowledge-canvas 的源代码和存储。
6. ✅ Python 单测、Node smoke、HTTP health、前端静态服务全部实跑通过。
7. ✅ 云端真实 Key 未伪造；使用 mock HTTP 验证云端通道。
8. ✅ 核心 Python/JavaScript 代码含中文注释或清晰命名。

### 遗留问题

1. 未绑定新项目远程仓库；如需推送，请提供目标 Git 地址。
2. 真实 Ollama/云端模型联网回归待用户提供可用模型和临时 Key。

## 七、M2 双层画布模型详情

### 改动文件

- `D:\ceshi\huabu\mindchat-knowledge-studio\web\board-model.js`
  - 新增独立 `conversationEdges` 与 `semanticEdges`。
  - `getVisibleEdges()` 只对视觉层去重，不删除任何关系数据。
  - 语义边关系枚举固定为延伸/相似/对比/依赖/补充。
- `D:\ceshi\huabu\mindchat-knowledge-studio\web\app.js`
  - 新增最小双层画布渲染、关系开关和卡片拖动重绘。
- `D:\ceshi\huabu\mindchat-knowledge-studio\web\index.html`
- `D:\ceshi\huabu\mindchat-knowledge-studio\web\style.css`
- `D:\ceshi\huabu\mindchat-knowledge-studio\tests\board-model.test.js`

### 验证输出

```text
Ran 5 tests in 0.037s
OK
board model smoke: 2 conversation edges + 2 semantic edges -> 3 visible edges
web index HTTP: 200
/board-model.js HTTP 200
/app.js HTTP 200
/style.css HTTP 200
```

### 自检 8 项

1. ✅ 模型对应《开发文档.md》第 2、5 节。
2. ✅ 未引入第三方前端框架或构建依赖。
3. ✅ 所有代码位于新项目目录，原项目未改动。
4. ✅ `conversationEdges`/`semanticEdges` 字段与文档一致。
5. ✅ 仅实现基础画布和去重，没有提前实现 Bridge/模型调用。
6. ✅ Python、Node、HTTP 页面均实跑通过。
7. ✅ 重合边“保留数据、隐藏重复显示”的决策已由测试覆盖。
8. ✅ 新增核心逻辑含中文注释。

### 遗留问题

无。M3 可以开始。

## 七、每日日志

### 2026-09-09

- 按用户要求启动全新独立项目规划。
- 建立《开发文档.md》，明确架构、数据模型、四格式导入、模型通道、里程碑和验收标准。
- 建立本进度文档，状态为 M0 进行中。
- M0 验收通过：开发文档、进度文档、README 均存在；原 qibu 的 `app.js/index.html/style.css/scripts` 无修改。
- M1 完成：建立独立导入适配层，四种文件和 Gemini 真实附件回归通过。
- M2 完成：建立独立双层画布模型和最小前端，验证关系边重合去重。
- M3 完成：建立新项目独立 Bridge API，dry-run 全链路和本地 Ollama 路由完成。
- M4 完成：增加云端 OpenAI 兼容通道，mock HTTP 验证鉴权和返回结构。
- M5 完成：增加独立多画布工作区、搜索、持久化和备份恢复。
- M6 完成：建立独立 Git 仓库，完成全量回归、启动说明和原项目隔离检查。

## 八、GitHub 上传与本地启动（2026-09-09）

### Git 状态

- 远程仓库：`https://github.com/liuxiu34/MindChat-Knowledge-Studio.git`
- 远程分支：`origin/main`
- 本地分支：`master` 跟踪 `origin/main`
- 最新已推送提交：`faa60a2 docs: finalize standalone project progress`

### 运行状态

- Bridge：`http://127.0.0.1:8791/health`，返回 `status=ok`。
- 前端：`http://127.0.0.1:8790/`，HTTP 200。
- Bridge 启动命令：`python -m bridge.server --port 8791`。
- 前端启动命令：`python -m http.server 8790 -d web`。

## 九、范围澄清与计划重排（2026-09-09）

### 用户澄清

新项目必须以 qibu 白板为完整基座，knowledge-canvas 只作为上层知识图谱扩展；当前最小前端原型功能不足，不能视为最终产品。

### 处理决定

1. 保留当前原型代码作为 M0/M1 的数据模型验证，不继续在其上追加零散功能。
2. 下一步先复制 qibu 的完整基座到新项目，逐项恢复 2D/3D、聊天、树视图、分支、附件、项目树、撤销重做、批注、诊断、备份和 i18n。
3. 基座回归通过后，再接入 knowledge-canvas 的知识卡片、语义边、图谱和自动分组。
4. 重新编号为 M1~M7；之前的 M1~M6 仅标记为“原型验证”，不代表最终功能完成。

### 当前断点

- 从 `D:\ceshi\huabu\mindchat-knowledge-studio\开发文档.md` 的“1.1 基座对齐要求”开始。
- 前置条件：保持 qibu 和 knowledge-canvas 只读；复制基座时只写新项目目录。

## 十、M1 qibu 基座迁移详情

### 改动文件

- `D:\ceshi\huabu\mindchat-knowledge-studio\client\index.html`
- `D:\ceshi\huabu\mindchat-knowledge-studio\client\style.css`
- `D:\ceshi\huabu\mindchat-knowledge-studio\client\app.js`
- `D:\ceshi\huabu\mindchat-knowledge-studio\client\scripts\*`
- `D:\ceshi\huabu\mindchat-knowledge-studio\client\tests\*`
- `D:\ceshi\huabu\mindchat-knowledge-studio\client\start-local-server.bat`
- `D:\ceshi\huabu\mindchat-knowledge-studio\README.md`

### 迁移原则

1. `client/` 是正式产品入口，以 qibu 完整白板为基座。
2. `web/` 仅保留 M0/M2 原型，不再作为正式入口。
3. knowledge-canvas 的知识能力后续以独立扩展模块接入 `client/`，不覆盖 qibu 基座。

### 验证输出

```text
/index.html HTTP 200
/style.css HTTP 200
/app.js HTTP 200
/scripts/importer.js HTTP 200
/tests/index.html HTTP 200
qibu base markup: present
node --check client/app.js: 通过
original qibu tracked code: clean
```

### 遗留问题

1. 尚未逐项完成浏览器交互回归；下一步从 M2 导入适配开始，但必须在 `client/` 基座上实现。
