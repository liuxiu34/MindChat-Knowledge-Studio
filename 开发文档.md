# MindChat Knowledge Studio 开发文档

> 版本：v0.1
> 建立日期：2026-09-09
> 项目性质：全新独立项目
> 原始参考项目：
> - `D:\ceshi\huabu`（qibu / MindChat Canvas）
> - `D:\BaiduNetdiskDownload\heima-ziliao\agent\knowledge-canvas`

## 1. 项目目标

重新实现一个独立的“对话白板 + 知识图谱工作台”，不修改、不依赖原 qibu 和 knowledge-canvas 的运行时代码。

用户可以：

1. 导入 `.txt`、`.md`、`.json`、`.docx` 四种文件。
2. 将聊天记录按原始顺序展示为对话卡片链。
3. 将对话内容提取为知识卡片。
4. 使用本地模型或云端模型分析知识卡片之间的语义关系。
5. 在同一个项目中同时查看对话顺序和知识关系，不产生语义混淆。
6. 编辑、拖拽、分组、搜索、导出知识卡片和图谱。

## 2. 明确边界

### 2.1 两种边必须独立

- **对话顺序边**：表示聊天记录中的先后顺序，只由 `conversation.parentId` 表示。
- **知识语义边**：表示相似、依赖、对比、延伸、补充，由独立的 `semanticEdges` 表示。
- 语义边不得改写 `parentId`。
- 如果两种边连接同一对卡片，数据都保留，但视觉层默认只显示一条，并在详情中标注“顺序关系与语义关系重合”。

### 2.2 原项目隔离

- 不直接修改 qibu 的 `app.js/index.html/style.css`。
- 不直接修改 knowledge-canvas 的 `whiteboard.html` 和 Python 脚本。
- 新项目拥有独立目录、独立存储前缀、独立启动命令和独立测试。
- 可复用设计思想和字段约定，但不复制大段运行时代码。

## 3. 用户流程

```text
导入 txt/md/json/docx
        ↓
统一解析为 Message[]
        ↓
对话白板：按顺序形成 conversation edges
        ↓ 用户点击“提取知识”
知识卡片抽取（本地 / 云端 / dry-run）
        ↓
知识图谱：生成 semantic edges
        ↓
同一界面双层展示、筛选、编辑、导出
```

## 4. 总体架构

```text
前端 SPA（原生 HTML/CSS/JS，零构建依赖）
 ├─ Import Adapter：四种文件格式
 ├─ Conversation Board：对话卡片与顺序边
 ├─ Knowledge Graph：知识卡片与语义边
 ├─ Workspace：项目/画布/搜索/分组/持久化
 └─ Model Settings：本地 Ollama / 云端 OpenAI 兼容
        │ HTTP localhost
        ▼
本地 Bridge Service（Python 标准库优先）
 ├─ normalize：docx/json/md/txt → 统一输入
 ├─ extract：问答块 → knowledge cards
 ├─ link：cards → semantic edges
 └─ provider router：local / cloud / dry-run
```

## 5. 统一数据模型

### 5.1 工作区

```json
{
  "version": 1,
  "workspaces": [{"id": "ws-1", "name": "默认工作区", "canvases": ["canvas-1"]}],
  "activeCanvasId": "canvas-1"
}
```

### 5.2 画布

```json
{
  "id": "canvas-1",
  "name": "默认画布",
  "cards": [],
  "conversationEdges": [],
  "semanticEdges": [],
  "groups": [],
  "annotations": []
}
```

### 5.3 卡片

```json
{
  "id": "card-1",
  "kind": "dialogue | knowledge",
  "role": "user | assistant | dialogue",
  "question": "",
  "content": "",
  "title": "",
  "category": "概念原理",
  "summary_q": "",
  "summary_a": "",
  "keywords": [],
  "source": "",
  "x": 0,
  "y": 0,
  "width": 320,
  "height": 220,
  "createdAt": ""
}
```

### 5.4 边

```json
{
  "source": "card-1",
  "target": "card-2",
  "relation": "延伸 | 相似 | 对比 | 依赖 | 补充",
  "reason": "",
  "confidence": 0.0
}
```

## 6. 四种文件输入规范

### txt

识别 `User/Assistant`、`you asked/gemini response`、中文说话人标签，并保留时间戳和多行内容。

### md

兼容普通 Q&A 标题、ChatGPT/Gemini/Claude 导出标记和 Markdown 文档段落。

### json

兼容：

- 顶层消息数组。
- `{messages: [...]}`。
- Gemini 风格 `contents: [{type: "text", content: "..."}]`。
- knowledge-canvas 风格 `cards/graph`，可直接导入知识卡片和语义边。

### docx

只读取 `word/document.xml` 的正文文本，转换成统一 Markdown；不执行宏、不解析外部链接、不写回原文件。

## 7. 模型通道

### 本地模型

- 默认：Ollama `http://127.0.0.1:11434`。
- 用于知识卡片提取和语义关联。
- 无模型时提供 dry-run 规则通道，保证流程可测试。

### 云端模型

- 使用 OpenAI 兼容 `/v1/chat/completions`。
- endpoint、model、API Key 由用户配置。
- API Key 只在内存和当前请求中使用，不写入 localStorage、项目文件或日志。

### 路由规则

- `dry-run`：规则提取/规则关联。
- `local`：Ollama。
- `cloud`：OpenAI 兼容云端。
- 前端只调用本地 bridge，避免浏览器直接处理 docx 和跨域模型请求。

## 8. 里程碑和验收标准

### M0 文档与骨架

- 新项目目录、开发文档、进度文档、README。
- 验收：原两个项目 Git 工作区无变化；新项目路径可独立运行。

### M1 四格式导入

- txt/md/json/docx 统一转 Message[]。
- 验收：四种真实样例分别得到正确消息数量和内容；Gemini 文件不再压成一张卡。

### M2 双层画布模型

- 完成对话卡片、顺序边、知识卡片、语义边的数据和基础渲染。
- 验收：同一对卡片的两种关系不互相覆盖；可分别隐藏/显示两类边。

### M3 本地 Bridge

- 完成 normalize/extract/link/health API。
- 验收：dry-run 全链路实跑；本地 Ollama 可切换；错误可解释。

### M4 云端模型通道

- 完成 OpenAI 兼容云端提取和关联。
- 验收：使用临时测试 endpoint/key 实跑；Key 不落盘；失败时不破坏当前画布。

### M5 工作区能力

- 多画布、搜索、分组、编辑、备份恢复、Obsidian Canvas 导出。
- 验收：刷新后数据完整；备份恢复前后节点和两类边一致。

### M6 收尾

- 浏览器回归、README、启动脚本、Git 提交。
- 验收：原项目无改动，新项目独立启动，全部 smoke tests 通过。

## 9. 非功能要求

- 优先原生 HTML/CSS/JS + Python 标准库，避免不必要构建链。
- 所有输入都视为不可信文本，Markdown/HTML 必须安全渲染。
- 长文本导入不能阻塞界面；分析过程要显示状态。
- 所有关键状态变更可恢复、可导出、可诊断。
- API 错误、模型不可用、格式错误都必须给出明确提示。

## 10. 当前不做

- 不把两个原项目的代码目录直接合并。
- 不让知识关系自动改变对话树。
- 不在第一版引入数据库、账号系统、远程文件同步。
- 不把 API Key 写入 URL、localStorage、Git 或导出文件。
